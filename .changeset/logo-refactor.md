---
"antd-multi-dashboard": patch
---

`Logo`: fix incorrect heading semantics, remove dead props (`imgSize.w`, `type="secondary"`, unused `FlexProps` spread), add `imgSrc`/`imgAlt`/`brandName`/`imgHeight` props for white-labeling, switch from `Typography.Title` to `Typography.Text` with `ellipsis`, default `href` to `/` instead of `#`.
