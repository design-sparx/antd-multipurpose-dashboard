---
"antd-multi-dashboard": patch
---

- fix(theme): add clean-style Layout tokens (light/dark) so sidebar and header use light/dark surfaces instead of antd's default dark navy, restoring readable menu text