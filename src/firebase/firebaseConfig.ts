/**
 * Firebase Configuration - DISABLED
 * Firebase is not used in mock-only mode
 * This file exports null values to prevent errors in code that imports it
 */

console.warn('[Firebase] Firebase is disabled in mock-only mode');

// Export null values for backward compatibility
export const analytics = null;
export const messaging = null;
