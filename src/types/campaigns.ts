export type CampaignAds = {
  id: string;
  ad_source: string;
  ad_campaign: string;
  ad_group: string;
  ad_type: string;
  impressions: number;
  clicks: number;
  conversions: number;
  cost: number;
  conversion_rate: number;
  revenue: number;
  roi: number;
  start_date: string;
};
